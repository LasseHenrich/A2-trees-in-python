{
    "dataset": "linear",
    "output": "linear-processed",
    "columns": [
        "x1",
        "x2"
    ],
    "steps": [
        { "output": "angle", "fn": "angle", "inputs": ["x1", "x2"] },
        { "output": "rotated", "fn": "rotate_x", "inputs": ["x1", "x2"], "angle": 45 }
    ],
    "select": [ "rotated"]
}
